# Deploys Angular Learning Lab at the site root and Storybook under /storybook.

name: Deploy app + Storybook to GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - 'projects/angular-learning-lab/**'
      - 'projects/my-lib/**'
      - 'package.json'
      - 'package-lock.json'
      - 'angular.json'
      - '.github/workflows/deploy-site.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: github-pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5

      - name: Build Angular app
        run: npm run build:app:pages

      - name: Build Storybook
        run: npm run build:storybook

      - name: Combine app and Storybook
        run: |
          rm -rf site
          mkdir -p site/storybook

          cp -R dist/angular-learning-lab/browser/. site/
          cp -R dist/storybook/my-lib/. site/storybook/

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: site

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
